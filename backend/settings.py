# Port where the server is running.
PORT = 8080
# If the application is in "debug" mode.
DEBUG = True
# It represents the state of the databases, "LOCAL" will use a variable within python itself to save the notes, "MONGO", "MYSQL" will use mysql as the database.
DATABASE = "LOCAL"
# Address of the database where mongodb runs.
MONGO_URL = "mongodb://localhost:27017"
